VITE_FB_API_KEY=
VITE_FB_AUTH_DOMAIN=
VITE_FB_PROJECT_ID=
VITE_FB_STORAGE_BUCKET=
VITE_FB_MESSAGING_SENDER_ID=
VITE_FB_APP_ID=
VITE_GMAPS_API_KEY=
# reCAPTCHA v3 site key for Firebase App Check. Optional: App Check stays off
# until this is set (see .claude/SECURITY-PLAN.md §2a). Provision in Firebase
# Console → App Check, register the web app, then add the key here + as a CI secret.
VITE_FB_APPCHECK_SITE_KEY=
# Web Push certificate public key for the /reminders/ page (Firebase Console →
# Project settings → Cloud Messaging → Web Push certificates). Public config.
# Also a CI secret of the same name.
VITE_FB_VAPID_KEY=
