// setupTiming.ts — how long a phone holds the setup section back after the
// screen loads before opening it by itself (decision 2026-09-18). 0 means no
// hold: the section is open from the start (the page tests use that).
export const SETUP_HOLD_MS = 1000
